// Adjust paths as necessary
export interface Player {
  idPlayer: number;
  contratEnd: string;
  contratStart: string;
  countryOfResidence: string;
  dateOfBirth: string;
  discordId: string;
  inGameName: string;
  jerseySize: string;
  leagalefullname: string;
  mailAdress: string;
  salary: number;
  socialMediaLinks: string;
  whatsappPhone: string;
  teamName: string; // Assuming you handle team association by name
  // Add any other attributes that are missing
}
